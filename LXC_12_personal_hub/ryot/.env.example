# Postgres (interno — nunca publicado no host)
POSTGRES_PASSWORD=your_ryot_postgres_password

# Token de admin do Ryot — gerar com: openssl rand -base64 32
SERVER_ADMIN_ACCESS_TOKEN=your_ryot_admin_token
